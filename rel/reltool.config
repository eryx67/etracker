{sys, [
       {lib_dirs, ["../deps", "../apps"]},
       {erts, [{mod_cond, derived}, {app_file, strip}]},
       {app_file, strip},
       {rel, "etracker", "1.0.0",
        [
         kernel,
         stdlib,
         sasl,
         crypto,
         observer,
         inets,
         mnesia,
         gproc,
         rlimit,
         ranch,
         cowboy,
         epgsql,
         etracker
        ]},
       {rel, "start_clean", "",
        [
         kernel,
         stdlib
        ]},
       {boot_rel, "etracker"},
       {profile, embedded},
       {incl_cond, derived},
       {mod_cond, derived},
       {excl_archive_filters, [".*"]}, %% Do not archive built libs
       {excl_sys_filters, ["^bin/.*", "^erts.*/bin/(dialyzer|typer)",
                           "^erts.*/(doc|info|include|lib|man|src)"]},
       {excl_app_filters, ["\.gitignore"]},
       {app, etracker, [{mod_cond, app}, {incl_cond, include}]},
       {app, hipe, [{incl_cond, exclude}]},
       {app, epgsql, [{mod_cond, app}, {incl_cond, include}]},
       {app, mimetypes, [{mod_cond, app}, {incl_cond, include}]}
      ]}.

{target_dir, "etracker"}.

{overlay, [
           {mkdir, "data"},
           {mkdir, "log/sasl"},
           {copy, "files/erl", "\{\{erts_vsn\}\}/bin/erl"},
           {copy, "files/nodetool", "\{\{erts_vsn\}\}/bin/nodetool"},
           {copy, "files/etracker", "bin/etracker"},
           {copy, "files/etracker.cmd", "bin/etracker.cmd"},
           {copy, "files/start_erl.cmd", "bin/start_erl.cmd"},
           {copy, "files/install_upgrade.escript", "bin/install_upgrade.escript"},
           {copy, "files/sys.config", "releases/\{\{rel_vsn\}\}/sys.config"},
           {copy, "files/vm.args", "releases/\{\{rel_vsn\}\}/vm.args"}
          ]}.
